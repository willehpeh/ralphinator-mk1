/**
 * Dashboard statistics data returned from the backend
 */
export interface DashboardStatistics {
  activeClientsCount: number;
  activeProjectsCount: number;
  pendingTasksCount: number;
  followUpsRequiredCount: number;
}
