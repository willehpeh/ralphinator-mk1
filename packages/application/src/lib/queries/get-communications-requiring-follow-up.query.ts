/**
 * Query to retrieve communications that require follow-up.
 * Returns communications where followUpRequired is true, sorted by most recent first.
 */
export class GetCommunicationsRequiringFollowUpQuery {
  // No parameters needed - retrieves all communications requiring follow-up
}
